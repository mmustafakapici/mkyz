Installation
============

To install the **mkyz** library, use the following command with `pip`:

.. code-block:: bash

   pip install mkyz

### Requirements
The library requires Python 3.6 or higher. Additionally, the following dependencies are needed:
- numpy
- pandas
- matplotlib
- scikit-learn

Make sure you have them installed. If not, they will be installed automatically when you install **mkyz**.
